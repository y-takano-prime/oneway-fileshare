# 開発ログ

## 集計

| 項目 | 時間 |
|------|------|
| 合計作業時間 | 2時間45分 |

---

## ログ

### 2026-06-15

| 開始 | 終了 | 所要時間 | 作業内容 |
|------|------|---------|---------|
| - | - | 1時間00分 | 環境構築（XAMPP Apache設定・Laravel 8インストール・PHP 7.4対応） |
| - | - | 0時間30分 | 要件定義書作成 |
| 16:30 | 17:45 | 1時間15分 | DB設計・マイグレーション作成、設計仕様書作成・要件定義書との整合性チェック |

**小計：2時間45分**

---

### 2026-06-16

| 開始 | 終了 | 所要時間 | 作業内容 |
|------|------|---------|---------|
| - | - | - | `docs/実装指示書.md` に基づき Step 1〜9 を実装（モデル・ミドルウェア・Mailクラス・コントローラー・ルーティング・ビュー・自動削除コマンド・Seeder）、動作確認後に初回コミット |
| - | - | - | URL管理画面の改善（URLコピー機能の不具合修正、送付メール文章の自動生成・コピー機能追加、相手先の名前欄を追加） |
| - | - | - | パスコード入力ステップを廃止し、メールアドレス確認＋OTPの2段階認証に変更（`docs/追加機能_パスコード廃止.md`） |
| - | - | - | ユーザー管理画面のバッジ配色を改善、ユーザー作成時のパスワード最小文字数を4文字に変更 |
| - | - | - | SMTP送信エラーの調査・解決（Kagoyaのユーザー名形式を特定し固定アカウント運用に確定）、メール本文の改行崩れ修正（text()送信に変更）、OTPメール送信者名を担当者名に変更 |


---

## 作業メモ

### 環境構築で発生した問題と解決策

- `httpd-ssl.conf` に `127.0.0.1 oneway-fileshare.local` が混入 → 削除で解決
- Laravelインストール時にPHP 8.4を要求される → `composer.json` に `"platform": {"php": "7.4.10"}` を追加し `composer update` で解決
- Apacheリスンポートを80→8080に変更済み

**2026-06-16**

- KagoyaのSMTPサーバーへの認証が `535 5.7.8 authentication failed` で失敗 → `MAIL_USERNAME` をメールアドレス形式（`y.takano@prime-gr.jp`）からKagoyaのアカウント名形式（`primegroup.ytakano`）に変更して解決
- クリップボードへのコピー機能（`navigator.clipboard`）が動作しない → HTTP環境（非secureコンテキスト）では使用できない仕様のため、`document.execCommand('copy')` へのフォールバックを追加して解決
- `.env` ファイルをエディタで編集・保存しても変更が反映されない問題が発生 → 原因不明（読み取り専用属性ではなかった）。最終的にユーザーの指示を受けてClaude Codeが直接編集して対応

### 環境情報

- Laravel: 8.x（PHP 7.4対応）
- PHP: 7.4.10（XAMPP）
- URL: http://oneway-fileshare.local:8080
